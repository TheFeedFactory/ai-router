apiVersion: v1
kind: Service
metadata:
  name: staging-ai-router
  labels:
    app: staging-ai-router
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: staging-ai-router
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http